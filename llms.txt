# Symbiote UI

> Agent-ready Web Components, layouts, themes, manifests, and UI contracts for professional Symbiote interfaces. No build step.

Symbiote UI lets agents and host apps assemble dense professional interfaces from reusable Web Components, layout shells, graph editors, chat surfaces, source/code panels, data displays, menus, controls, and Cascade themes. Use the published package exports and provider metadata instead of importing implementation paths or cloning host-product UI.

## Key resources

- [README](https://github.com/rnd-pro/symbiote-ui/blob/main/README.md): package overview, quick start, features, entry points, and related projects
- [CHANGELOG](https://github.com/rnd-pro/symbiote-ui/blob/main/CHANGELOG.md): release history and current unreleased changes
- [custom-elements.json](https://github.com/rnd-pro/symbiote-ui/blob/main/custom-elements.json): full Web Component catalog with properties, attributes, events, and theme aliases
- [discover.js](https://github.com/rnd-pro/symbiote-ui/blob/main/discover.js): provider discovery command used by agents and build tooling

## Core docs

- [Entry Points](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/entry-points.md): Node-safe, browser-safe, UI, layout, canvas, theme, manifest, and WebMCP imports
- [Runtime UI Construction](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/runtime-ui-construction.md): runtime trees, component descriptors, transactional UI construction, and rollback expectations
- [Integration Contracts](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/integration-contracts.md): provider contracts, host boundaries, and agent-facing metadata
- [Cascade Theme](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/cascade-theme.md): theme params, aliases, recipes, and scoped provider theming
- [Layout and Spatial](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/layout-and-spatial.md): layout shells, routers, sidebars, panel menus, canvas, graph, XR, and spatial surfaces
- [Agent UI Principles](https://github.com/rnd-pro/symbiote-ui/blob/main/docs/agent-ui-principles.md): professional UI reuse rules for agent-built interfaces

## Agent skills

- [symbiote-ui](https://github.com/rnd-pro/symbiote-ui/blob/main/skills/symbiote-ui/SKILL.md): runtime UI controller, WebSocket protocol, dynamic component registry, intent orchestration, and WebMCP bridge
- [symbiote-ui-theming](https://github.com/rnd-pro/symbiote-ui/blob/main/skills/symbiote-ui-theming/SKILL.md): Cascade theme system, theme presets, motion layer, and state-to-Cascade CSS induction
- [symbiote-library-adapter](https://github.com/rnd-pro/symbiote-ui/blob/main/skills/symbiote-library-adapter/SKILL.md): adapter rules for mapping external UI libraries into Symbiote provider metadata

## Schemas

- [runtime-ui-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/runtime-ui-v1.json): runtime UI tree schema
- [agent-intent-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/agent-intent-v1.json): transactional agent intent schema
- [component-descriptor-v2](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/component-descriptor-v2.json): component descriptors with SSR and WebMCP metadata
- [theme-rule-block-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/theme-rule-block-v1.json): composable Cascade theme rule blocks
- [project-package-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/project-package-v1.json): portable project package metadata
- [project-transaction-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/project-transaction-v1.json): project graph transaction metadata
- [graph-layout-quality-v1](https://github.com/rnd-pro/symbiote-ui/blob/main/schemas/graph-layout-quality-v1.json): exact graph layout audit input, report, policy, coverage, and finding contract

## Component and layout guidance

- Use `symbiote-ui/ui` for browser Web Components and shared UI modules, or a narrow component subpath (for example `symbiote-ui/canvas/node-canvas`, `symbiote-ui/canvas/canvas-graph`, `symbiote-ui/layout/panel-layout`, `symbiote-ui/control/segmented-control`, `symbiote-ui/ui/media`) to register only the elements a host uses; see the Entry Points doc for the full list.
- Use `symbiote-ui/layout` for layout shells, routers, sidebars, project tabs, panel menus, and region orchestration.
- Use `symbiote-ui/canvas` or `symbiote-ui/graph` for graph explorers, node canvases, structured/flat graph modes, graph metadata, transactions, and deterministic layout-quality reports.
- Use `symbiote-ui/chat` for chat composers, transcripts, message lists, sidebars, and message models.
- Use `symbiote-ui/display` for source viewers/editors, code blocks, data tables, metrics, banners, event feeds, and output previews.
- Use `symbiote-ui/themes`, `symbiote-ui/tokens`, and `symbiote-ui/manifest` for Cascade theme metadata, aliases, and provider catalogs.

## Graph explorer guidance

Register `graph-explorer-shell`, `node-canvas`, `canvas-graph`, and `graph-node` when building graph editing or project graph interfaces. Use `createGraphViewModeController` from `symbiote-ui/ui` or `symbiote-ui/canvas` to switch structured and flat renderer visibility, fit/focus state, path style, and host-owned flat graph models. Use `flatNodeIds` when fitting several `canvas-graph` nodes in flat mode; `flatNodeId` is single-node focus and can enter a hierarchical parent group. `canvas-graph` reads `--sn-canvas-graph-*` and `--sn-graph-type-*` aliases from the Cascade theme, so host apps should pass semantic node types instead of local canvas color constants.

Before accepting a settled graph layout, call `analyzeGraphLayout` from `symbiote-ui/graph` with a JSON-compatible snapshot or run `symbiote-ui layout-audit <snapshot.json>`. Read the operation, numeric domain, policy fields, report invariants, per-rule payload schemas/types/units, and `graph-layout-quality-v1` input/output schema from `symbiote-ui discover` → `manifest.graphAnalysis`. Treat `fail` and `incomplete` as blocking; use stable finding rule IDs and involved node/edge IDs for repairs. `layout.numeric-underflow` means a non-zero derived center delta, direct metric, or aggregate ratio materialized as zero and the result is intentionally incomplete. `coverage.checks.*.required` counts entity pairs, `status` is `complete` or `skipped-budget`, and `budgetCost` is the worst-case primitive geometry cost. Do not infer quality from screenshots, mutate geometry inside the analyzer, or apply product-specific article/media rules as library defaults.
